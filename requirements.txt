# TachiDUBB Studio — application dependencies
# Python 3.11 is the supported Windows baseline for this fork.
# Install the matching CUDA runtime first; see requirements-cuda128.txt.

# Core server
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.18,<1
httpx>=0.27,<1

# Agent integration (MCP server for Claude Code etc.). Optional but tiny.
# CLI works without it; only `tools/tachidubb_mcp.py` requires this.
mcp>=1.12,<3

# Audio/video I/O
soundfile>=0.13,<1
numpy>=2.1,<3
pydub>=0.25.1

# Language processing
nltk>=3.9,<4

# Video download
yt-dlp>=2025.1.0

# Transcription
# WhisperX provides the word-alignment API used by pipeline/transcriber.py.
# Its current release requires the CUDA 12.8 Torch 2.8 stack in the companion
# constraints file.  Do not install it before that stack.
whisperx==3.8.6
faster-whisper==1.2.1
torchcodec>=0.6,<0.8

# Voice cloning — Tier 1 (best quality, 8+ GB VRAM)
voxcpm==2.0.3

# Voice cloning — Tier 2 (good quality, 3+ GB VRAM)
# Install to enable F5-TTS as a VoxCPM alternative:
# f5-tts>=0.9.0

# Neural TTS fallback (no GPU, no cloning, always available)
edge-tts>=7.2,<8

# Vocal separation — Tier 1 (recommended, ships with torch)
# Install to enable Demucs (htdemucs_ft model):
# demucs>=4.0.0

# Vocal separation — Tier 2 (alternative to demucs)
# audio-separator>=0.17.0

# Speaker diarization (optional, needs HF_TOKEN in .env or config-user.json)
# pyannote.audio>=3.1

# PyTorch — install separately with correct CUDA version:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
